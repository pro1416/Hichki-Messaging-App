<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.praveshsingh.chatapp">

    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".activities.splashscreen"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activities.login_activity"
            android:theme="@style/AppTheme"/>
        <activity
            android:name=".activities.signup_activity"
            android:theme="@style/AppTheme"/>
        <activity
            android:name=".activities.member_list_activity"
            android:theme="@style/AppTheme"/>
        <activity
            android:name=".activities.Friend_RequestList_activity"
            android:theme="@style/Theme.MaterialComponents.NoActionBar" />
        <activity
            android:name=".activities.FriendList_activity"
            android:theme="@style/AppTheme"/>
        <activity
            android:name=".activities.ChatWindow"
            android:theme="@style/AppTheme"/>
        <activity
            android:name=".activities.dashboard"
            android:theme="@style/AppTheme"/>
        <activity
            android:name=".activities.NoUI"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>